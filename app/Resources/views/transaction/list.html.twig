<table class="striped comp-transactions js-transactions {% if fart_joke %}fart{% endif %}"
       data-list="{{ path('list_json_transactions', {'id': account.id}) }}"
       data-create="{{ path('create_json_transactions', {'id': account.id}) }}"
       data-delete="{{ path('data_delete_transaction') }}"
       data-check="{{ path('data_check_transaction') }}"
       data-fart="{{ asset('sounds/fart') }}"
>
    <thead>
        <tr>
            <th>Date</th>
            <th class="hide-on-med-and-down">ID</th>
            <th>Label</th>
            <th class="center-align hide-on-med-and-down">Pointage</th>
            <th class="right-align">Débit</th>
            <th class="right-align">Crédit</th>
            <th class="hide">id</th>
            <th class="hide">timestamp</th>
        </tr>
    </thead>
</table>
