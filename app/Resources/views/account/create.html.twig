<div class="card">
    <div class="card-content">
        {{ form_start(form) }}
            <div class="row">
                <div class="col m8">
                    {{ form_row(form.name) }}
                </div>
                <div class="col m2 offset-m1">
                    <br />
                    <br />
                    {{ form_row(form.starred) }}
                </div>
            </div>
        {{ form_end(form) }}
    </div>
</div>
