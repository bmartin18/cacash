{% extends "base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{% block title %}Réinitialiser mon mot de passe{% endblock title %}

{% block body %}
    <div class="row">
        <div class="col s12 m6 offset-m3 l6 offset-l3 xl4 offset-xl4">
            <div class="card">
                <div class="card-image">
                    <img src="{{ asset('images/card/cacash.jpg') }}">
                </div>
                <div class="card-content">
                    <p>{{ 'resetting.check_email'|trans({'%tokenLifetime%': tokenLifetime})|nl2br }}</p>
                </div>
                <div class="card-action">
                    <a href="{{ path('fos_user_security_login') }}" class="btn btn-flat">Connexion</a>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
